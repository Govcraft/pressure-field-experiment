%% ============================================================================
%% DISCUSSION
%% ============================================================================

\section{Discussion}
\label{sec:discussion}

Decentralized symmetric-stigmergic coordination wins where strict hierarchical control collapses, and the way it collapses is itself evidence for the symmetric extension.

\subsection{Why Decentralized Coordination Wins in This Regime}

Three factors explain the advantage in the regime where it holds. First, centralized coordination spends budget on coordination rather than on the problem: a manager-worker protocol issues several model calls per applied patch, so it attempts fewer patches per tick and explores less. This matches the large-scale finding that coordination overhead can become a net cost~\cite{kim2025scaling} and that most inter-agent messages are redundant~\cite{zhang2025agentprune}. Second, the domain is locally decomposable: fixing a conflict in one time block rarely creates one in a distant block, so greedy local descent suffices and global planning buys nothing, consistent with the observation that language-model agents are stronger at reading environmental state than at reasoning about other agents~\cite{agashe2025coordination}. Third, addressing several distinct high-pressure regions per tick lets progress happen wherever it can, so no single hard region stalls the whole run.

\subsection{Two Failure Modes: Rejection Loops and Legality Stalls}
\label{sec:failure-analysis}

Each coordination paradigm fails through its own characteristic mechanism, and the two mechanisms locate what escalation can and cannot repair.

Hierarchical control collapses on the harder instances through the \emph{rejection loop}. Strict-greedy control always selects the highest-pressure region, but a region is highest-pressure precisely because it resists improvement. When the proposed patch fails validation, the region stays highest-pressure and is selected again, so the controller re-attacks the one region it cannot fix while the rest of the artifact goes untouched. The rate of this pathology is reported with the failure statistics: $13\%$ of hierarchical runs apply no patch at all, and roughly $94\%$ of the manager's proposals are rejected by the validation gate.

The loop is a selection pathology, not a capability deficit, and the protocol makes that distinction measurable. The hierarchical baseline shares the system's model-escalation trigger (Section~\ref{sec:experiments}), so when it stalls it eventually proposes with the largest model in the chain, and it still re-attacks the same region: escalated capability arrives at the same greedy choice. Two things are needed to escape, and the symmetric system supplies both. Field-based concurrent dispatch keeps progress flowing on the regions that are tractable while the hard region waits out its inhibition window, and the environment-to-agent escalation operator of Section~\ref{sec:symmetric-stigmergy} reallocates capability where the field has stalled, the share of solves requiring escalation rising with difficulty (Section~\ref{sec:escalation}). The hierarchical failure is therefore not a tuning accident but a structural consequence of coupling all capability, however escalated, to a single greedy selection rule; the experiments make that collapse the empirical case for coordinating selection through the field.

Pheromone-field coordination fails through a different mechanism, the \emph{legality stall}, and its unsolved easy instances share one signature: the run descends to a single unscheduled meeting with zero overlaps ($P = 1.0$) and stays there (all $20$ of its unsolved easy instances end at exactly $P = 1.0$). Placing the last meeting requires first moving a placed meeting that blocks its attendees, but every legal first step of that rearrangement is non-improving: evicting the blocker to the pool raises $P$ by one, and an in-block swap that places one meeting while evicting another leaves $P$ unchanged, so the strict-decrease gate rejects both. This is greedy improving-move dynamics terminating at a local minimum of the potential rather than its global minimum~\cite{monderer1996potential}, realized in the gate. Escalation cannot repair it, because no amount of proposal quality makes a strictly improving single-region patch exist: in the stalled trials the proposal stream continues at full rate with the model escalated to the top of the chain, and the rejected proposals are dominated by exactly the swap and eviction moves that would begin the two-step escape ($33{,}821$ rejected proposals at $\Delta = 0$ and $50{,}177$ at $\Delta = +1$ across the grid, together $47\%$ of all rejections). The gate that contains model error (Section~\ref{sec:method}) is the same rule that forbids these neutral intermediate states; containment and greed are one mechanism, and the legality stall is its price.

The taxonomy is then simple. Rejection loops are selection failures, cured by coordinating selection through the field. Capability stalls are proposal failures, cured by the $ASU$ escalation arrow. Legality stalls are action-space failures, and nothing in the present operator set cures them: an atomic evict-and-place move spanning two regions would clear the plateau under the same strict gate, at the cost of widening the single-region patch to a two-region transaction (Section~\ref{sec:discussion}, future work).

\subsection{Limitations}

\textbf{Propagation is one hop only.} The environment-propagation operator applies a single hop per tick from regions' own pressures, not multi-hop accumulated diffusion. This keeps the model near the agent-based end of the continuum and bounds how far a hot region's influence spreads in one tick; richer diffusion over the temporal chain, and over non-temporal region relations, is unexplored here.

\textbf{Problem scale is small for a swarming method.} The instances are deliberately sized so that hierarchical and conversational baselines remain feasible to run, which is the opposite of the high-combinatorial regime where stigmergy is usually argued to excel. The regime characterization should therefore be read as a lower bound on where decentralized coordination helps, not as a test at swarm scale.

\textbf{Absolute solve rates are bounded by capability and by the gate.} Even the pheromone-field strategy leaves many hard instances unsolved ($16.7\%$ solved on hard); with 0.5b--3b models, convergence analysis points to model capability as one limit on hard problems, and the legality stall of Section~\ref{sec:failure-analysis} as a second limit that larger models would not remove. Larger models would plausibly raise absolute rates on capability-bound instances, an effect this study does not measure.

\textbf{Evidence is from a single domain.} Results on meeting room scheduling may not transfer to domains lacking measurable local quality signals or locality. The mechanism itself, the sensed field, evaporation, propagation, and two-stage validation, is domain-agnostic, but only the pressure function carries domain knowledge, and a mis-specified pressure function invites Goodhart-style gaming, where agents reduce the measured pressure without improving true quality.

\subsection{Societal Considerations}

Decentralized coordination shifts three deployment concerns relative to hierarchical control, and each is addressable. Accountability diffuses, because several agents may improve a region with no record of which one ``owned'' the outcome, so regulated deployments need an audit log of patch provenance, the pressure signal at proposal time, and the validation result; the provenance log in region state already records this. Metric gaming is a standing risk, because agents optimize the pressure function rather than true quality, so the function should use multiple orthogonal axes and be audited against human judgment. Explainability is mechanistic but not causal: the system records that a region was high-pressure and a patch reduced it, not why that patch was chosen, which suits domains where outcomes are cheaply verified and is weaker where human justification is required. These are deployment requirements around a simple mechanism, not changes to the mechanism itself.

\subsection{Future Work}

\begin{itemize}
\item \textbf{Richer environment propagation.} Extend the one-hop operator to multi-hop diffusion and to non-temporal region relations, and measure where on the agent-to-equation continuum the best operating point lies.
\item \textbf{Escaping legality stalls.} Either widen the action space to atomic two-region moves, which pass the strict gate unchanged, or relax acceptance to admit non-worsening patches under a cycle guard; the first preserves the gate's containment property, the second trades it for mobility.
\item \textbf{Scale and capability.} Test at swarm-scale problem sizes and with larger models, to separate the regime boundary from the small-model ceiling.
\item \textbf{Learned and multi-artifact pressure.} Learn pressure functions from solution traces, and extend to coupled artifacts where a patch in one shifts pressure in another.
\end{itemize}
